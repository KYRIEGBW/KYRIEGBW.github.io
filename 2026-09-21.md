# 2026-09-21

## 个人工作台.html 改版
- 移除手机内置浏览器（iframe / 多标签 / X-Frame 引导卡全删）：文件夹内链接与手机磁贴恢复为普通 `<a target="_blank">`，点击直接新标签打开目标网站，工作台本身不跳转。
- 删除手机底部 dock（微博/CSDN/抖音/喜马拉雅四枚），其中部分站点并入第 1 页快捷入口（现 10 个磁贴）。
- 手机改为「两页」：`.p-pager > .p-track`（width:200%）+ 两个 `.p-page`，translateX 切换，支持鼠标拖动/触屏滑动切换（横向阈值 16%），底部圆点指示 + 点击跳转；第 2 页为音乐播放器，切换时 `.screen.dark` 让整屏（含状态栏）渐变为深色。
- 屏幕高度固定 `height:176cqw`（原 min-height:184cqw），使手机整体按固定宽高比缩放。
- 新增统一卡片调节大小模块：`attachResize(el, key, opt)`，右下角 `.rz` 手柄拖动改宽高，双击复位，尺寸写入 localStorage 键 `wt-card-size-v1`。
  - 网格卡片宽度会换算成 `grid-column-end: span N`（ceil 进位）避免互相重叠；`.grid` 加了 `align-items:start`。
  - 文件夹展开时释放自定义高度（height:auto）防止内容被裁，收起时恢复用户高度；展开状态下只调宽度。
  - 手机只用宽度调节（高度由 cqw 容器查询等比推导）。
- 便签改为 flex 列布局，`.note-edit{flex:1;min-height:140px}`，便签卡片调高即可获得更大编辑区。

## 个人工作台.html 二次改版（玻璃拟态 · 深色极光）
- 背景整体换成深色极光：多层 radial-gradient（青/紫/粉/绿）+ `linear-gradient(165deg,#0a0f22,#131a3d,#0b1424)`，配 5 个 blur 光斑；UI 全面玻璃拟态（`.glass` 基类：rgba(255,255,255,.075) + backdrop-filter blur(22px) saturate(165%) + 1px 亮边 + inset 高光），文字改浅色 `#eaf0ff`。
- 布局重排：`.shell` 网格「左手机列（sticky）+ 右主内容」；右侧顺序为 顶部 hero 玻璃条 → 今日待办 / 随手记 两列 → 站点导航 → footer。≤1060px 手机移到顶部并居中。
- 只有「随手记」保留自由调节大小（`.rz` 手柄，尺寸存 `wt-note-size-v1`，双击复位），文件夹与手机不再可调整尺寸（删掉通用 attachResize / grid span 换算模块）。
- 手机第 2 页由音乐播放器改为**日历**：月视图（周一为首列）、‹ • › 切月/回今天、今天高亮渐变、点击选中日期、底部显示「今天 · X月X日」与「已选 …」；音乐播放器（TRACKS / 网易云外链 / 音频相关代码）全部删除。
- 新增**今日待办**：输入框回车或 ＋ 添加、点方框打勾（划线+变灰）、悬停 ✕ 删除、「清已完成」按钮、徽标显示 `已完成 / 总数`；数据存 localStorage 键 `wt-todo-v1`。
- 手机屏高改为 `height:163cqw`；快捷入口 10 枚（去掉音乐磁贴，补哔哩哔哩）。

## 重要：用户把文件回退到了「第一版改版」（浅色 + 音乐播放器 + 全卡片可调大小）
20:38 左右 `个人工作台.html` 被外部回退（mtime 变更），磁盘上是带音乐播放器的浅色版。后续改动都基于这一版，不要再假设深色玻璃版还在。

## 该版后续修复（用户反馈）
- 手机底部加翻页按钮：`.p-nav` = `‹` + 圆点 + `›`（`#pPrev` / `#pNext`），到头时加 `.off`（半透明禁用）；滑动翻页阈值从 16% 降到 10%。屏高 176cqw → 186cqw 容纳导航条。
- 笔记拉窄时头部文字被挤掉：`.note` 加 `container-type:inline-size`，`@container (max-width:340px)` 隐藏 `.tip`；`.note-head` 允许换行，`.clear` 设 `flex-shrink:0` 保证「清空」按钮始终可见，`.tip` 用省略号收缩。
- 版面重排（对齐）：新增 `.shell` 网格 `clamp(238px,21vw,330px) minmax(0,1fr)`，DOM 顺序必须是 **header → aside.phone-col → .wrap**（网格自动排布依赖 DOM 顺序，header 通栏必须在最前，否则会被挤到第二行）。手机列 sticky，与右侧笔记/网格顶部对齐。
- 统一左右边界：去掉 header 的 `max-width:1140px`、`.note-wrap` 与 `.stage` 的横向 padding，使顶部信息条、笔记卡、站点网格左边缘对齐。
- `fitPhone` 改为给 `.phone-col` 加 `.no-stick`（不再作用于 .phone-wrap）。

## 最终版（用户说上一版没保存，整套重做 · 已落盘）
深色极光背景 + 玻璃拟态 UI 的完整版重新写入 `个人工作台.html`，并保留上面两项修复：
- 骨架：`.shell` 网格「手机列 sticky ‖ 主内容」，DOM 顺序 header → aside → main（header 通栏必须最前）；主内容顺序：待办+笔记两列 → 站点导航 → footer。
- 手机两页：主屏（时钟+10 磁贴）/ 日历（月视图、‹ • › 切月、今天渐变高亮、点选日期）；底部 `.p-nav` = ‹ + 圆点 + ›，到头变灰禁用，滑动阈值 10%。屏高 178cqw。
- 今日待办：增删/打勾/清已完成，localStorage `wt-todo-v1`。
- 只有随手记可调大小：localStorage `wt-note-size-v1`，`@container (max-width:340px)` 隐藏 badge 保证「清空」按钮常驻。
- 音乐播放器相关代码已彻底移除（TRACKS / audio / pm* 全删）。
- 坑：CSS Grid 自动排布依赖 DOM 顺序，通栏 header 若写在 aside 之后会被挤到第二行。

## 21:20 增量改动（在最终版基础上）
- 卡片调节大小改为通用 `attachResize(el,key,minW,minH)`，三张卡都可拖：今日待办 `#todoCard` / 随手记 `#noteCard` / 站点导航 `#sitesCard`；尺寸统一存 localStorage `wt-card-size-v2`（对象，按 key 分子项），双击手柄复位。
  - 关键：`flex-basis` 必须与 `width` 一起设置，否则 `.cols` 里 `flex:1 1 330px` 会忽略 width。
  - 拖过的卡加 `.sized`：`align-self:flex-start`（不再等高拉伸）、`overflow:hidden`、内部列表/网格 `flex:1;max-height:none;overflow-y:auto`。
  - `.cols` 由 CSS Grid 改为 `display:flex;flex-wrap:wrap`，避免卡片加宽时溢出网格轨道。
- 今日待办新增「全选 / 取消全选」按钮 `#todoAll`（`.mini-btn.on` 高亮）。
- 随手记内容持久化：localStorage `wt-note-v2`（input 防抖 250ms + beforeunload），刷新保留；「清空」同步写空串。
- 外链统一拦截：document 捕获阶段 `a[href]` → preventDefault + `window.open(href,'_blank','noopener,noreferrer')`，保证当前工作台页面绝不跳转；被拦截时用临时 `<a>` 兜底。
- 手机快捷入口删除 微博 / CSDN / 哔哩哔哩 / 喜马拉雅，新增「豆包」(http://dy.pbgia.com/bmdb)，现 7 枚；`.p-apps .p-app:last-child:nth-child(odd){grid-column:1/-1}` 让奇数枚时最后一个占整行。

## 21:28 刷新入场动画
- 新增 `@keyframes cardIn`（透明 + 上移 30px + 缩放 .955 → 归位）与 `.anim` 类（.66s，`cubic-bezier(.2,.86,.3,1.06)`，`backwards` 填充），延迟档 `.d0~.d6`（.03s 起，每档 +80ms）。
- 应用顺序：hero d0 → 手机 d1 → 今日待办 d2 → 随手记 d3 → 站点导航 d4 → footer d6。
- 子元素错峰：文件夹 `380+i*55ms`、待办条目 `i*45ms`、手机磁贴 `320+i*60ms`。
- 坑：`.anim` 规则必须写在 CSS **末尾**（`.hero` / `.phone-wrap` 自带 `animation:rise`，同特异度靠后者胜出，否则被覆盖）。

## 21:43 卡片交互大改（八向缩放 + 长按拖动 + 刷新还原）
- **不再持久化卡片尺寸**：删掉 `SIZES/saveSizes`，启动时清 `wt-card-size-v1/v2`、`wt-note-size-v1`；刷新即回到默认排版（待办/笔记内容仍各自持久化）。
- `setupCard(el,minW,minH)` 取代 `attachResize`：状态存 `cardState` Map（`{w,h,x,y}`，w/h 为 null 表示自适应），`applyCard()` 统一写 width/flexBasis/height/`transform:translate(x,y)` 并切 `.sized`/`.moved` 类。
- **八向手柄**：`HANDLES` 数组生成 8 个 span（`.rz-edge` 四条边 6px 细条 + `.rz-corner` 四角 16px），hz/vt 方向系数；关键是**拖左/上边时要同时补偿位移**：`s.x = x0 + (w0 - w)`、`s.y = y0 + (h0 - hh)`，否则左边/上边不会动。
- **长按拖动**：卡片 pointerdown → 320ms 定时器 → 加 `.lifted`（浮起阴影+描边）+ `body.dragging` + setPointerCapture；排除 `input,button,a,.rz,.note-edit,.todo-list,.grid`；移动超 6px 取消长按；松手用 `suppressClick`（document 捕获阶段吃掉一次 click）防止误触按钮。
- 手柄默认 `pointer-events:none`，`.resizable:hover` 时才 `auto`，否则透明手柄会挡住卡片内容点击。
- 每张卡头部加 `⟲ 还原` 按钮（`data-reset="cardId"`）→ `resetCard()` 清空 w/h/x/y。

## 21:57 回退 + 修 bug（当前最终形态）
- 用户要求回到上一版交互：**只保留右下角单个 `.rz` 手柄**（经典斜线样式），删除八向手柄 / 长按拖动 / `HANDLES` / `lifted` / `moved` / `dragging` / `suppressClick`。保留：双击手柄还原、`⟲ 还原` 按钮、刷新还原布局、入场动画。
- **重大 bug（必记）**：给卡片设 `flex-basis` 时，父容器必须是**横向 flex** 才代表宽度。站点导航的父容器 `.main` 是 `flex-direction:column`，`flex-basis:900px` 被当成**高度** → 一拉宽度整张卡暴涨。
  - 修法：`applyCard()` 里用 `getComputedStyle(parent).flexDirection.indexOf('row')===0` 判断，只在横向时同步 `flex-basis`（`.cols` 的 `flex:1 1 330px` 必须靠 basis 才生效），纵向时只设 `width`。该判断是运行时的，≤820px `.cols` 变 column 也自动正确。
- 宽度上限 `maxW = parent.clientWidth - 2`，保证「拉多大显示多大」且不溢出。
- 22:08 小改：手机快捷入口 `Canva 可画` → `微信传输助手` (https://filehelper.weixin.qq.com/，📤，微信绿 #07c160)；注意站点导航「素材」分类里的 Canva 仍在，不要误删。删除页面底部 `<footer>` 整行说明文字。手机磁贴仍为 7 枚。

## 22:13 底部动态波纹
- 新增 `.wave-row`（`.shell` 的 grid 子元素，`grid-column:1/-1`）→ 通栏，**左端 = 手机左缘、右端 = 内容右缘**；因为 grid 第 2 行高度 = max(手机, 主内容)，波纹自然落在手机最下端之下。
- 同时把 `.phone-wrap` 的 `margin-inline:auto` 改成 `margin-right:auto`（靠左），保证手机左缘与波纹左端严格齐平（长窗口下 46vh 小于列宽时才会体现）。
- `<svg id="waveSvg">` viewBox `0 0 1200 130` + `preserveAspectRatio="none"`，`width:100%`、`height:clamp(66px,10vh,132px)`、`overflow:hidden`、`pointer-events:none`。
- 波纹由 JS 生成 5 层正弦线（k1/k2 为「每 1200 宽度的完整周期数」，**周期必须整除 1200 才能无缝**），路径画 2 屏（0~2400），用 CSS `@keyframes waveMove{to{translateX(-1200px)}}` 平移一屏；各层 `animation-duration` 16~30s + 部分 `animation-direction:reverse` 形成错流。发光层用 `<filter><feGaussianBlur stdDeviation=6/10>`。
- 坑：CSS transform 里 `px` 在 SVG 子元素上按 user unit 计算（配合 `transform-box:view-box`），所以 -1200px 正好一屏。

## 22:16 手机高度对齐站点导航底部
- `.phone-wrap` 的 `width:min(100%,46vh)` 改为 `width:100%`，真实宽度交给 JS：`fitPhone()` 用 `mainCol.getBoundingClientRect().height / PHONE_RATIO` 反算宽度，并 clamp 到 `[210, 左列宽度]`，使手机总高 = 右侧主内容高度 → 两者底部齐平。
- `PHONE_RATIO`（总高÷宽 ≈1.854）**实测获取**：临时设 `width:300px` 读 `offsetHeight`（不能用 `getBoundingClientRect()`，会被入场动画的 scale 影响），再还原。
- 用 `ResizeObserver` 观察 `.main`（rAF 节流）+ resize/load 回调，展开文件夹或拖动站点导航大小时手机会自动跟着变。
- 窄屏（≤1060px）手机独立成行，不需要对齐：媒体查询里用 `width:min(100%,46vh) !important;margin-inline:auto` 覆盖 JS 的内联宽度。

## 22:26 手机拉长到波纹上沿 + 波纹去线头
- 手机不再是「底部 = 站点导航底部」，而是**一路拉长到波纹最高点**：`need = rowH + rowGap + waveTopPad()`，`waveTopPad` 由波纹脚本算出的 `WAVE_MINY`（最高可见点，用户单位）乘 `svgRect.height / viewBoxH` 得到。
- **关键防死循环**：手机变高会撑高 grid 行 → 把波纹顶下去 → 手机再变高。解法是先用 `phoneCol.style.height = max(主列高, 360) + 'px'` **钉死左列高度**（右列决定行高），手机再高也只是「溢出」到波纹那一行的透明区域，不影响行高。手机溢出的部分要 `z-index:2` 压在波纹上方（`position:sticky` 本身是定位元素）。
- 左列宽度上限 320px → 344px（`clamp(240px,21.5vw,344px)`）：否则拉长后宽度被卡住够不到波纹。
- 波纹几何整体上移：viewBox `0 0 1200 92`、`CY=46`、CSS 高 `clamp(56px,8.5vh,104px)`、各层 base 重排（blur 7/5），保证最高点 6.4、最低点 77.6（不裁切）。
- **去掉两端的「线头」**：① 路径从「画 2 屏 (0~2400)」改为**画 3 屏 (-1200 ~ 2600)**，动画只平移一屏，可见窗口恒在中间一屏内，两端线头永远进不了画面；② `.wave` 加 `mask-image:linear-gradient(90deg,transparent 0,...,#000 9%,#000 91%,...,transparent 100%)` 让两端柔和淡出（`-webkit-` 前缀同写）。

## 22:35 手机底部与站点导航底部严格齐平（闭环反算）+ 文件夹只露 3 行
- 用户反馈「手机最下端与右边站点导航最下端对齐」。**用截图逐像素测量**（Pillow 取列向颜色跳变 + 用灵动岛 `27.2cqw` 标定 cqw）发现：手机实际渲染比例 ≈ **1.909**，而不是理论 1.854（`100cqw` 宽，`178cqw` 屏 + `7.4cqw` 内边距）。因为比例偏大，按 1.854 反算会偏高，手机反而比站点导航底部低约 11px。
- **改法：不再用固定 `PHONE_RATIO` 反算，改成闭环**——先按理论值给个初值写 `width`，再读 `phoneWrap.offsetHeight/offsetWidth` 迭代 3 次：`next = clamp(rowH * cw / h, 210, avail)`，直到手机总高 == 右列总高。目标从「波纹最高点」改回 **`rowH = 主列高度`**（= 站点导航底边）。
- 目标变了之后 `waveTopPad/WAVE_MINY/minY`（波纹最高点 padding 那套）全部删除，`shellEl` 也删。
- `.wave-row` 加 `margin-top:calc(-1 * clamp(14px,1.9vh,22px))` 抵掉网格行距 → 波纹上沿紧贴站点导航/手机底边，中间不留空档（两端仍有淡出遮罩）。
- 「今日待办往上缩一点」：`.todo-empty` 的 `padding:22px 0` → `10px 0`，右侧主列高度少约 24px（.cols 里待办是最高的那张，它决定行高），手机随之等比缩小但依旧严格对齐。
- 站点导航手风琴：新增 `VISIBLE_ROWS = 3` + `openCap(body)`——用 `subs[2].offsetTop + offsetHeight - body.offsetTop + 6` 算高度（+6 露一丝第 4 行暗示可滚），不足 3 个则返回 `scrollHeight`；替换原来 `max(180, innerHeight*0.24)` 的封顶，其余站点在卡片内滚动。
- 环境坑：本机 bash shim 已损坏（`dirname`/`head`/`rm`/`tail` 都不可用，PowerShell 工具输出被吞），Node 与 Python 仍可用 → 文件操作用 `node -e "fs.unlinkSync(...)"`，图像分析用 `python -m pip install Pillow` + PIL。
- 坑：bash shim 还会误解析含 `()` 的 node -e 表达式，**复杂校验脚本要写成 .js 文件再 node 跑**，不要塞在 `-e` 里。

## 22:45 波纹调细调色 + 手机磁贴调整
- 用户嫌波纹「线太粗、离站点导航太近、颜色与背景不搭」：
  - `.wave-row` 的 `margin-top` 从「抵掉行距的负值」改为正值 `clamp(22px,3.2vh,40px)` → 与站点导航/手机底边拉开呼吸距离（叠加网格 rowGap 后总间距约 36~62px）。注意这不影响 `fitPhone()`（它只依赖 `.main` 高度），手机底部仍与站点导航底部齐平。
  - 线宽整体收细：主亮线 3.4→1.4，辉光带 20/8→13/5，细线 1.8/1.5→1/0.9；模糊 sig 7/5→6/4。
  - 配色改为与背景极光同源的低饱和青/紫/粉：`rgba(56,189,248,.16)`、`rgba(139,92,246,.20)`、主亮线 `rgba(165,214,255,.72)`、`rgba(167,139,250,.42)`、`rgba(244,114,182,.30)`。原来的纯 `#3b82f6` 蓝在深色极光上过于跳。
  - 验算：5 层含线宽+模糊扩散的 y 范围都在 viewBox `0~92` 内（11.4~75.9），不裁切。
- 手机磁贴（仍 7 枚）：`DeepSeek` → `UU远程`(https://uuyc.163.com/，🖥️，靛蓝→青 #6366f1→#22d3ee)；`豆包` 与 `微信传输助手` **互换位置**，现顺序 百度 / UU远程 / WorkBuddy / 腾讯微云 / GitHub / 豆包 / 微信传输助手。
- 站点导航「AI 工具」分类里另有一个 DeepSeek 链接（第 531 行），用户只说手机界面，**未删**，保留待确认。

## 22:52 手机尺寸冻结 + 顶部「空气小组件」
- **手机不再跟随右列内容变化**：删掉 `.main` 上的 `ResizeObserver` 整段。`fitPhone()` 现在只在 `resize` / `load` / 400ms / 1000ms / 1800ms 时跑——拖动今日待办或随手记改大小时，左列电话保持不动（`phoneCol.style.height` 钉在首次算出的值，行高由右列决定，手机下方留白）。窗口尺寸变化时仍重新适配并保持「手机底边 = 站点导航底边」。
  - 为什么这样做：用户要的是「调待办大小时手机别跟着变」，而保持底部对齐在数学上不可能同时成立，所以选择冻结 + 只在视口变化时重算。
- **顶部信息条新增「空气小组件」**：`.air-widget`（`flex:1 1 150px;min-width:0;height:clamp(42px,5vh,56px);border-radius:999px;overflow:hidden;cursor:crosshair`），插在 `.hero-l` 与 `.hero-r` 之间，吃掉中间那段空白；`@media (max-width:880px)` 隐藏。
  - 内容为 `<canvas id="airCanvas">`，纯手写 2D 动画：n = clamp(W/48, 3, 9) 颗泡泡从底部上浮（vy 10~24 px/s + `sin` 左右摆动），鼠标附近 60px 有斥力把泡泡吹开并高亮（`hot` 余温），**点击戳破** → 10 颗碎屑 + 一圈涟漪 + 随机拟声词（啵！/呼～/咕噜…），然后从底部补一颗新的；戳空则冒一圈「呼～」。
  - 配色复用极光调色板 `[56,189,248] [139,92,246] [236,72,153] [52,211,153] [245,158,11] [124,156,255]`；泡泡用 `createRadialGradient` 做玻璃感 + 左上高光点。
  - 支持 `prefers-reduced-motion: reduce`：不跑 rAF，只静态渲染一帧。
  - 坑：canvas 要按 `devicePixelRatio` 放大再 `ctx.setTransform(DPR,0,0,DPR,0,0)`，否则高分屏发虚。
