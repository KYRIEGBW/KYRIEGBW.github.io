# 2026-09-20

- 依据 Downloads/地址栏.html（浏览器书签导出）制作「个人工作台.html」：13 个文件夹分类、40+ 网址。
- 按用户要求改为手风琴交互：点击文件夹原地展开二级菜单（max-height 过渡 + 子项交错滑入动画），每项均为超链接新标签打开；展开时自动收起其他文件夹。
- 顶部保留实时时钟、时段问候与全局搜索（搜索时以卡片形式展示全部匹配网址）。

- 晚间迭代：修复 `--glass-blur;` 非法 CSS 变量写法导致的编辑器报错（改为每处直接写 backdrop-filter/-webkit-backdrop-filter）；背景升级为动态漂浮光斑（.bg/.blob，blur 70px，drift 动画 26-40s），内容层 z-index:1 置于光斑之上。
- 用户中途提供自己的玻璃风格版本并要求以其为准覆盖（含自加的"百度"链接）；最终版本为玻璃风格+动态光斑。头像 avatar.jpg 与页面同目录。

- 布局改为左手机 UI + 右内容（sticky 手机，≤1160px 时上下堆叠）；随后第五轮调整一次完成：手机 344→382px 宽、屏幕 648→704px（灵动岛/状态栏/时钟/头像/ Dock 同步放大）；背景色系由蓝紫粉换为青绿/天蓝/蜜桃/珊瑚/薰衣草（body 渐变同步）；光斑速度 11-17s 提到 6-9s（迷你光斑 9→6.5s）；毛玻璃加强：--card 透明度 .58→.44、blur 24→32px saturate 185%、hover/open/sub 底色降低；手机屏幕内容由文件夹联动列表改为「快捷入口」2×3 应用磁贴（QUICK 数组：百度/DeepSeek/腾讯文档/哔哩哔哩/GitHub/Canva，新标签打开），删除 renderPhone 的文件夹联动逻辑。

- 响应式改造（解决不同屏幕比例不一致）：.phone-wrap 设为 CSS 容器（container-type:inline-size），宽度 clamp(300px, min(25vw,44vh), 450px)，手机内部所有尺寸改用 cqw 容器查询单位整体等比缩放；.wrap 改 flex:1 1 640px（max 1180px）弹性填充；断点 1160→1020px（堆叠时手机 clamp(300px,min(72vw,60vh),430px)）；标题/头像/时钟/便签高度/间距全部 clamp() 流式；文件夹展开高度 JS 改为随窗口高度自适应（max(180, innerHeight*0.22)）。

- 手机快捷入口按用户截图圈选调整：删「腾讯文档、哔哩哔哩」，换成 WorkBuddy（workbuddy.cn，🤖 橙红渐变）和 腾讯微云（☁️ 青蓝渐变）；底部 Dock 由 4 个装饰 emoji（素材/影视/工具/博客）改为 4 个真实链接按钮：微博👀、CSDN💻、抖音🎵（深色）、喜马拉雅🎧，新标签打开，.dapp 加 text-decoration:none。（用户随后自改 WorkBuddy 链接为 workbuddy.cn/app）

- 新增手机内置浏览器：.screen 内加 .p-browser 覆盖层（地址栏 p-bar：🏠回主屏 + 域名显示 + ↗新标签；p-view 内 iframe，sandbox 防止顶层跳转），openInPhone(url) 统一入口；右侧文件夹链接、手机磁贴、Dock 全部改为在手机屏内打开（preventDefault + openInPhone），不再跳新标签；加载提示 4 秒未完成时提示"该网站可能禁止内嵌显示"。注意：X-Frame-Options 限制是浏览器行为，GitHub/百度/B站等多数大站无法 iframe 内嵌，只能靠 ↗ 兜底。

- 修复"手机显示不全/比例不好"：① 手机宽度改 clamp(250px, min(24vw,40vh), 430px)、sticky top 降为 clamp(10px,2.5vh,26px)，新增 no-stick 保险（JS fitPhone：手机比窗口高时去掉 sticky 改静态，保证滚动可见）；② 手机内置浏览器加整页缩放：iframe 按 1024px 虚拟桌面宽度 scale 塞进屏幕（fitMode，仿手机浏览器 980px 视口逻辑），p-bar 新增「1:1/适应」切换按钮，窗口 resize 时重算；磁贴同步放大（pe 9.4cqw、字号 3.7cqw）。

- 手机浏览器升级多标签+缩放调节：① 缩放按钮改循环档位 ZOOMS=['fit',0.75,1,1.25]（适应/75%/100%/125%），非适应档按 w/s 渲染逻辑宽再 scale(s)；② 新增 .p-tabs 标签栏（域名胶囊 + ✕ 关闭，已开过的网址自动切回不重复开，关完自动回主屏，goHome 清空）；③ 抖音拒绝 iframe 内嵌（"拒绝了我们的连接请求"属网站 X-Frame-Options 策略），Dock 抖音加 data-ext="1" 走新标签直开通道，其他拒嵌网站靠 ↗ 兜底。

- 内嵌体验再改（用户反馈）：① 默认档改「手机」= 按手机屏真实宽度 1:1 渲染（响应式网站自动出手机版页面），档位循环 手机→75%→适应(1024桌面缩放)；② 新增 BLOCKED 域名黑名单（douyin/bilibili/weibo/github/xigua/youku/iqiyi/mgtv/v.qq.com），命中时 iframe 置空、手机屏内显示 .p-block 引导卡（🔒 + "新窗口打开 ↗" 按钮 window.open 当前标签 url），抖音撤掉 data-ext 改走此引导流；data-ext 机制保留在 dock 监听里备用。注意 JS 区块多次被用户手改，Edit 前必须重读定位。

- 手机内音乐播放器（用户澄清："还是手机，里面加一个按钮可以变成音乐播放器界面"——保留手机，非替换）：① 「快捷入口」标题行加 .pm-open 🎵 圆钮（p-sec-row 布局，播放中脉冲动画）；② .screen 内加 .p-music 深色覆盖层（z-index:7，低于 .p-browser 的 8，手机上滑入动画），含旋转黑胶唱片（avatar.jpg 当封面）、曲名/歌手、9 条均衡器动画、进度条（可点击跳转）、⏮⏯⏭ 控制、音量滑块、4 首歌单；③ 音乐引擎为 Web Audio 生成式（无需音频文件）：TRACKS 4 首不同 BPM 风格（晚安电台 Lo-Fi 72/晨间漫步 Chill 88/深夜代码 Ambient 60/元气满格 Upbeat 104），和弦进行 I-VI-IV-V + 五声音阶琶音 + 底鼓/镲片，scheduler 90ms 轮询 + 0.35s lookahead；暂停用 ac.suspend()（进度冻结原地续播），切歌重置节拍器；返回键只收起界面音乐继续放，🎵 按钮脉冲提示播放中。教训：误把整台手机替换成播放器（CSS chunk 1a 被换），用户纠正后已逐字还原；@keyframes eq/spin 随还原丢失，此次与播放器 CSS 一起补回。

- 播放器换真实歌曲（用户："放上赵雷的歌"）：Web Audio 合成引擎整体替换为 <audio> + 网易云外链播放。歌曲 ID 经 WebSearch + WebFetch music.163.com/api/artist/6731（赵雷歌手ID=6731，hotSongs JSON）核实：成都436514312/鼓楼447926067/南方姑娘202373/画202369/理想29567189/少年锦时29567192/三十岁的女人29567191/我们的时光29567193/阿刁447925059/我记得1974443814，共10首。外链格式 https://music.163.com/song/media/outer/url?id=ID.mp3（<audio> 直接可播，无 CORS 问题）。新增：pm-block 外链失效引导卡（版权限制→"新窗口打开↗"去网易云歌曲页，error 事件触发、mErrShown 防重复）、歌单项 .pi-ext ↗ 按钮随时可跳网易云、ended 自动切下一首、loadedmetadata/timeupdate 驱动进度条。已知限制：VIP/版权受限歌曲外链可能返回占位提示音而非报错，无法程序化识别——靠 ↗ 按钮兜底。
